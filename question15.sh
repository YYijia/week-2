git checkout main
# Merge branch1 into main
git merge branch1
# Note: If there are merge conflicts, resolve them manually.
# Add the resolved files
git add .
# Commit the resolved merge
git commit -m "Merge branch1 into main with conflicts resolved"